<div class="bg-brand w-full">
    <div class="md:w-1/2 w-full mx-auto flex flex-col items-center justify-center">
        <ul class="flex w-full my-8 justify-around md:justify-start items-center divide-x divide-gray-lighter">

            {%- for entry in collections.about -%}
                <li class="px-6 md:my-8">
                    <a class="border-b-2 border-transparent {{ 'text-highlight hover:border-highlight' if entry.url == page.url else 'text-gray-light hover:border-gray-light' }}  " href="{{ entry.url | url }}">{{ entry.data.title | upper }}</a>
                </li>
            {%- endfor -%}

        </ul>
    </div>
</div>