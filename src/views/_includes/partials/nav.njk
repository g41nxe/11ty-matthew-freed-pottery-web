<div class="nav container mx-auto">
    <nav class="fixed z-50 w-full py-2 md:bg-transparent bg-blue-800">
        <ul class="flex w-100 justify-around md:justify-start items-center">
        {% set navPages = collections.all | eleventyNavigation %}        
        {%- for entry in navPages %}        
            <li class="md:mr-6 md:py-8">
                <a class="border-b-2 border-transparent hover:border-brand text-brand {{ 'font-bold' if entry.url == page.url }} " href="{{ entry.url | url }}">{{ entry.title | upper }} </a>
            </li>
        {%- endfor %}
            
            <li class="md:mr-6 md:py-8">
                <a href="{{ global.socialmedia.services.instagram.url | url }}" class="text-brand border-b-2 border-transparent hover:border-brand">
                    <i class="fab fa-instagram"></i>                
                </a>
            </li>

            <li class="md:mr-6 md:py-8">
                <a href="{{ global.shop.url | url }}" class="text-brand border-b-2 border-transparent hover:border-brand">
                    <i class="fas fa-shopping-bag"></i>                
                </a>
            </li>

        </ul>
    </nav>
</div>